# AWS Lambda profile
# Lambda handler is configured automatically by quarkus-amazon-lambda-http extension

# DynamoDB configuration - uses URL connection client for Lambda cold start optimization
quarkus.dynamodb.sync-client.type=url
